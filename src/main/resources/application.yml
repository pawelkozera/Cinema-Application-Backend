spring:
  config:
    import: classpath:paypal.properties
  datasource:
    url: jdbc:postgresql://localhost:5432/cinemadatabase
    username: postgres
    password: adminna131zp
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
      properties:
        hibernate:
          format_sql: true
      database: postgresql
      database-platform: org.hibernate.dialect.PostgreSQLDialect

paypal:
  client:
    id: ${paypal.client.id}
    secret: ${paypal.client.secret}
  mode: sandbox